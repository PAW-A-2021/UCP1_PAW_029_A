@model IEnumerable<UCP1.Models.Administrator>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaAdministrator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomorTelepon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPerusahaan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaAdministrator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomorTelepon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPerusahaan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAdministrator">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAdministrator">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAdministrator">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
